# Chronos example environment configuration

# Logging (optional)
RUST_LOG=chronos=info
# File logging
CHRONOS_LOG_FILE=chronos.log
CHRONOS_LOG_MAX_SIZE_MB=10
CHRONOS_LOG_MAX_FILES=3

# Security: SQL auth tokens (optional)
# Admin token: can run both reads and writes over SQL gRPC
CHRONOS_AUTH_TOKEN_ADMIN=change-me-admin
# Read-only token: only SELECT is allowed, writes will be denied
CHRONOS_AUTH_TOKEN_READONLY=change-me-readonly
# Convenience token used by the Chronos client REPL in distributed mode
CHRONOS_AUTH_TOKEN=change-me-client

# Security: TLS / mTLS for gRPC (optional)
# When all three variables below are set, Chronos enables TLS
# for its gRPC server and clients, and uses the CA for mTLS.
CHRONOS_TLS_CERT=/path/to/server.crt
CHRONOS_TLS_KEY=/path/to/server.key
CHRONOS_TLS_CA_CERT=/path/to/ca.crt
# Override SNI / domain if needed (default: localhost)
CHRONOS_TLS_DOMAIN=localhost
